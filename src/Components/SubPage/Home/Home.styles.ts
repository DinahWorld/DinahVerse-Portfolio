import {makeStyles} from 'tss-react/mui';

export default makeStyles()((theme) => ({
    home: {
        height: "100vh",
        padding: "10px"
    },
    navbar: {
        paddingLeft: "20px",
        paddingRight: "20px"
    }
}))